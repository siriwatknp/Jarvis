{
  "functions": {
    "runtime": "nodejs20",
    "source": ".",
    "predeploy": [
      "yarn --prefix \"$RESOURCE_DIR\" prebuild:puppeteer-extra",
      "yarn --prefix \"$RESOURCE_DIR\" build"
    ]
  }
}
